name: Keep Backend Awake

on:
  schedule:
    - cron: "*/5 * * * *"   # every 5 minutes
  workflow_dispatch:         # allows manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API
        run: |
          echo "Pinging Frontend to keep backend alive..."
          curl -s https://findemy-wdn9.vercel.app/
          echo "Ping completed"
