name: Keep Backend Awake

on:
  schedule:
    - cron: "*/5 * * * *"   # every 5 minutes
  workflow_dispatch:         # allows manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API
        run: |
          echo "Pinging backend..."
          curl -s https://findemy-backend-1.onrender.com/health
          echo "Ping completed"
